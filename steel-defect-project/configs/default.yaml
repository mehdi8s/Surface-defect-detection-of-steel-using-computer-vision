seed: 42
num_workers: 2
device: auto

data:
  dataset_root: ../NEU-DET
  image_size: 224
  batch_size: 32

train:
  epochs: 25
  learning_rate: 0.0005
  weight_decay: 0.0001
  early_stopping_patience: 6
  model_name: resnet18

output:
  run_name: baseline_resnet18
  output_root: ./outputs
